import ftReact from "@rabarbra/ft_react";
import { apiClient } from "../api/api_client";

const Main = (props) => {
    return (
        <div>
            <h1>Camaguru</h1>
        </div>
    );
};

export default Main;